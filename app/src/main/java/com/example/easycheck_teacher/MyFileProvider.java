package com.example.easycheck_teacher;

import androidx.core.content.FileProvider;

public class MyFileProvider extends FileProvider {
    // No additional code needed, as FileProvider is just a subclass of ContentProvider
    // You can customize it further if needed, but for basic usage, no additional code is required
}
